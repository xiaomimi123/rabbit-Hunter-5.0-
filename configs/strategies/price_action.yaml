name: price_action
version: "0.3.1"
params:
  swing_proximity_atr: 2.0
  doji_proximity_atr: 1.0
  pin_bar_shadow_body_multiplier: 2.0
  pin_bar_shadow_range_fraction: 0.6
  # v0.3.1: rebalance to preserve v0.2's engulfing-heavy signal shape.
  # v0.3.0's flatter distribution (0.45/0.25/0.15/0.15) diluted the strong
  # signals — max score of 0.8+ dropped from 49 trades to 0 trades.
  w_confirmed_engulfing: 0.55
  w_swing_proximity: 0.25
  w_directional_pin_bar: 0.10
  w_doji_at_swing: 0.10
